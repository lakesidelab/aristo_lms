require 'test_helper'

module AristoLms
  class MarkTest < ActiveSupport::TestCase

    # setup do
    #   @mark = aristo_lms_marks(:one)
    # end
    #
    # test "the truth" do
    #   assert true
    # end
    #
    # test "should not insert without training id" do
    #   mark = Mark.new(training_id: nil, user_id: @mark.user_id)
    #   assert_not mark.save
    # end
    #
    # test "should not insert without training id" do
    #   mark = Mark.new(training_id: @mark.training_id, user_id: nil)
    #   assert_not mark.save
    # end
  end
end
